# X4newH Documentation Set

Copy the six Markdown files in `docs/` into the repository and commit them to `X4newH`.

The documentation was updated against verified branch HEAD:
`eedab26b3827cc2bc94be413b7afd04d02670f24`
